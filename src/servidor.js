import axios from "axios"
import Router from "axios"
import 'dotenv/config'



const express = require('express')
const app = expres (() => {
    return console.log("escuchando en el puero 5147")
})

use.Router(listen)